using LogicMine;

namespace Sample.LogicMine.Shop.Service.Mine.SalesSummary
{
    // A custom request for a sales summary.  To keep things simple there are no parameters but in reality a request 
    // like this would probably include something like a data range.
    public class SalesSummaryRequest : Request
    {
    }
}